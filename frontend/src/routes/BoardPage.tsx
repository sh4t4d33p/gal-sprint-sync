import { Typography, Box } from '@mui/material';

export default function BoardPage() {
  return (
    <Box>
      <Typography variant="h4" fontWeight={700} mb={2}>Sprint Board</Typography>
      <Typography color="text.secondary">This is the main board. (Coming soon!)</Typography>
    </Box>
  );
} 